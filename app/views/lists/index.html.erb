<div class ="container">
<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<!-- for face detection -->
<%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>

<!-- for face detection -->
<%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>

  <h1>These are all your lists</h1>
  <p> <%= link_to "Create List", new_list_path %> </p>
    <ul> <%@lists.each do |list|  %>
      <li> <%= list.name %> <%= link_to list.name, list_path(list) %>
      <% end %>
      </li>
    </ul>
</div>
